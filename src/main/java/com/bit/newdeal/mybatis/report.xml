<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.bit.newdeal.dao.reportDao">
  
  	<select id="selectReport" resultType="Report">
  	 select * from report
  	</select>
  	
  	<insert id="insertReport" parameterType="Report">
  		insert into board(rno, istTo, isFrom, targetTypeCode, targetCode,
  		reasonCode, rContent, processCode )
  	   values(#{rno}, #{isTo}, #{isFrom}, #{targetTypeCode}, #{targetCode}, #{reasonCode}, #{rContent}, #{processCode})
  	</insert>
  	
  	<update id="updateReport" parameterType="java.util.Map">
  	   update report set 
  	   <if test="num == 1">
  	   processCode='R01'
  	   </if>
  	   <if test="num == 2">
       processCode='R02'
       </if>
       where rno=#{rno}
  	</update>
  
  </mapper>